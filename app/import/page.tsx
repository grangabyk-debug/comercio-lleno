'use client'
import Link from 'next/link'
import { useState } from 'react'
export default function Import(){const [file,setFile]=useState('');return <main className="app"><header className="appbar"><Link className="brand" href="/dashboard">Comercio <span>Lleno</span></Link><Link href="/">← Caja</Link></header><div className="content"><h1>Importar productos</h1><p className="muted">Subí el Excel exportado desde tu sistema actual. El importador se encargará de normalizar códigos, precios y stock.</p><div className="importBox"><div className="drop">📥<b>{file||'Elegí tu archivo Excel'}</b><small>Formatos previstos: .xlsx y .xls</small><input type="file" accept=".xlsx,.xls,.csv" onChange={e=>setFile(e.target.files?.[0]?.name||'')}/></div><div className="mapping"><b>Columnas que vamos a reconocer</b><span>Código de barras · Producto · Categoría · Precio · Costo · Stock</span></div><button className="primary" disabled={!file}>Vista previa e importar</button></div></div></main>}
