'use client'

// Analytics remain intentionally disabled until the production measurement
// stack and consent flow are configured together. This avoids loading a
// third-party tracker before the launch privacy configuration is complete.
export default function PostulaClarity(){
  return null
}
