'use client'
import Link from 'next/link'
import { useState } from 'react'
import BrandLogo from '../BrandLogo'
const demo=[['779000000001','Detergente 750 ml','Limpieza',2500,24],['779000000002','Lavandina 2 L','Limpieza',1800,18],['779000000003','Esponja multiuso','Bazar',950,50]]
export default function Products(){const [q,setQ]=useState('');const rows=demo.filter(x=>x.join(' ').toLowerCase().includes(q.toLowerCase()));return <main className="app"><header className="appbar"><Link className="brand" href="/dashboard" aria-label="Comercio Lleno"><BrandLogo size={32}/></Link><Link href="/">← Caja</Link></header><div className="content"><div className="pagehead"><div><h1>Productos</h1><p className="muted">Catálogo, precios, códigos y stock.</p></div><Link className="primary" href="/import">Importar Excel</Link></div><input className="tableSearch" value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar producto o código de barras"/><div className="table"><div className="tr th"><span>Código</span><span>Producto</span><span>Categoría</span><span>Precio</span><span>Stock</span></div>{rows.map(x=><div className="tr" key={x[0]}>{x.map((v,i)=><span key={i}>{i===3?`$ ${Number(v).toLocaleString('es-AR')}`:v}</span>)}</div>)}</div></div></main>}
